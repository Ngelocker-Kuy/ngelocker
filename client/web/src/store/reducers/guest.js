const initialState = {
  guests: []
}

export default function guestsReducers(state = initialState, actions) {
  switch (actions.type) {
    case 'TEST':
      
      break;
  
    default:
      return state
  }
}