const initialState = {
  admin: []
}

export default function adminReducers(state = initialState, actions) {
  switch (actions.type) {
    case 'TEST':
      
      break;
  
    default:
      return state
  }
}